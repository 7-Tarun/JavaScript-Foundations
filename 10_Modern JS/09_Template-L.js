//Template Litrals.
//It is a modern way to make or define string, where we can directly use Variables, Expressions, and multi-lines string without the use of '+'.

let name = "Dr. Stephen Strange";
let age = "21";

let result = `My name is ${name} and I am ${age} years old.`;
console.log(result);

let city = "Jaipur";
let temp = 26;

console.log(`Today's temperature in ${city} is ${temp} degree Celcius.`);