const name = 'Stephen Strange';     //Constant, This will remain contant can not be changed.
let age = 24;       //Mutable, Value can be change.

// age = 27;  This would work.
// name = 'Strange';    This cause an Error.

console.log(name);
console.log(age);

console.log(typeof age);    //Show the type of varible.

let game = 'PUB G';
let kills = 5;

console.log(`Game ${game} and Kills ${kills}`);