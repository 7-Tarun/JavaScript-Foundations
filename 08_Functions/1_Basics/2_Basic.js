//A Function where the output is depend upon the input.

function message(msg){         //The input variable between the brackets that is "msg" called parameter.
    console.log(msg);
}
message("I am Heisenberg");         //Value which is sent to the functon is called argument. (Both are most lickly same)
message("Hello World!");

//Explantion: The msg has been sent to the fuction and the function print the msg which is "I am Heisenberg".
//Note:- We can pass multiple arguments.

//Passing multiple argumnets.
function tarun(yo,hi,bye){
    console.log(yo);
    console.log(hi);
    console.log(bye);
}
tarun("My","Name is","Tarun");